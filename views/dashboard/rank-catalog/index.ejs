<%- include("../../admin/_head.ejs", { pageTitle }) %>
<%- include("../../admin/_topbar.ejs") %>
<%- include("../../admin/_sidebar.ejs") %>

<div id="wpcontent">
  <div id="wpbody-content">
    <div class="wrap">

      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2"><i class="fa-solid fa-layer-group fa-fw me-2"></i>Rank Catalog</h1>
        <div class="btn-toolbar gap-2">
          <a href="/ranks" target="_blank" class="btn btn-sm btn-outline-secondary">
            <i class="fa-solid fa-arrow-up-right-from-square me-1"></i>View Page
          </a>
          <a href="/dashboard/webstore/categories" class="btn btn-sm btn-outline-secondary">
            <i class="fa-solid fa-tags me-1"></i>Categories
          </a>
          <a href="/dashboard/rank-catalog/create" class="btn btn-sm btn-primary">
            <i class="fa-solid fa-plus me-1"></i>Add Entry
          </a>
        </div>
      </div>

      <%- include("../../admin/_notices.ejs") %>

      <% if (!categoryGroups || !categoryGroups.length) { %>
        <div class="alert alert-info">
          No categories yet.
          <a href="/dashboard/webstore/categories" class="alert-link">Create a category</a> first —
          every product belongs to one.
        </div>
      <% } else { %>

        <% categoryGroups.forEach(function(group) { %>
          <div class="card shadow-sm mb-4">
            <div class="card-header d-flex justify-content-between align-items-center">
              <span class="fw-semibold">
                <%= group.name %>
                <% if (group.visible === false) { %>
                  <span class="badge bg-warning text-dark ms-2" title="This whole category is hidden from the ranks page, whatever each product below is set to.">
                    <i class="fa-solid fa-eye-slash me-1"></i>Category hidden
                  </span>
                <% } %>
              </span>
              <span class="badge bg-secondary"><%= group.items.length %> <%= group.items.length === 1 ? 'entry' : 'entries' %></span>
            </div>
            <% if (!group.items.length) { %>
              <div class="card-body text-muted small">
                Nothing in this category yet.
                <a href="/dashboard/rank-catalog/create">Add an entry</a> and pick it.
              </div>
            <% } else { %>
            <div class="table-responsive">
              <table class="table table-sm table-hover align-middle mb-0">
                <thead class="table-light">
                  <tr>
                    <th style="width:32px">#</th>
                    <th>Name</th>
                    <th>Stripe Prices</th>
                    <th>Perks</th>
                    <th style="width:96px">Visible</th>
                    <th style="width:110px"></th>
                  </tr>
                </thead>
                <tbody>
                  <% group.items.forEach(function(entry) { %>
                    <tr class="<%= entry.visible === false ? 'opacity-50' : '' %>">
                      <td class="text-muted"><%= entry.sortOrder %></td>
                      <td>
                        <% if (entry.imageUrl) { %>
                          <img src="<%= entry.imageUrl %>" alt="" style="height:24px; object-fit:contain;" class="me-2">
                        <% } %>
                        <strong><%= entry.displayName %></strong>
                        <% if (entry.description) { %>
                          <div class="text-muted" style="font-size:0.78rem;"><%= entry.description %></div>
                        <% } %>
                      </td>
                      <td>
                        <% if (entry.stripePriceIds && entry.stripePriceIds.length) { %>
                          <% entry.stripePriceIds.forEach(function(pid) { %>
                            <code class="small d-block"><%= pid %></code>
                          <% }) %>
                        <% } else { %>
                          <span class="text-muted small">— none</span>
                        <% } %>
                      </td>
                      <td class="text-muted small"><%= entry.perks ? entry.perks.length : 0 %> perks</td>
                      <td>
                        <%# One click to publish or pull a product, without reopening the form. %>
                        <form method="post" action="/dashboard/rank-catalog/<%= entry.id %>/visibility" class="d-inline">
                          <input type="hidden" name="visible" value="<%= entry.visible === false ? 'on' : 'off' %>">
                          <button type="submit"
                                  class="btn btn-sm <%= entry.visible === false ? 'btn-outline-secondary' : 'btn-outline-success' %>"
                                  title="<%= entry.visible === false ? 'Hidden — click to show' : 'Visible — click to hide' %>">
                            <i class="fa-solid <%= entry.visible === false ? 'fa-eye-slash' : 'fa-eye' %> fa-fw"></i>
                          </button>
                        </form>
                      </td>
                      <td class="text-end">
                        <a href="/dashboard/rank-catalog/<%= entry.id %>/edit" class="btn btn-sm btn-outline-secondary me-1">
                          <i class="fa-solid fa-pencil fa-fw"></i>
                        </a>
                        <form method="post" action="/dashboard/rank-catalog/<%= entry.id %>/delete" class="d-inline"
                              onsubmit="return confirm('Delete <%= entry.displayName %>?')">
                          <button type="submit" class="btn btn-sm btn-outline-danger">
                            <i class="fa-solid fa-trash fa-fw"></i>
                          </button>
                        </form>
                      </td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
            <% } %>
          </div>
        <% }) %>

      <% } %>

    </div>
  </div>
</div>

<%- include("../../admin/_footer.ejs") %>
