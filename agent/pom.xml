<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.anchorlight</groupId>
    <artifactId>zander</artifactId>
    <version>2.0.1</version>
    <packaging>pom</packaging>

    <modules>
        <!-- <module>zander-waterfall</module> -->
            <!-- Decomissioned Waterfall in v1.3.0 -->
        <module>zander-velocity</module>
        <module>zander-hub</module>
        <module>zander-auth</module>
        <module>zander-addon</module>
        <!-- zander-pgm is deferred out of 2.0.0 — see the feat-deferred-features branch -->
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <defaultGoal>clean install</defaultGoal>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>copy-artifacts</id>
                        <!--
                            verify, not package. maven-shade-plugin binds to
                            package and replaces the module's jar with the
                            shaded one, and this inherited execution ran before
                            it — so the last module in the reactor had its
                            pre-shade jar collected here, missing every bundled
                            dependency. Earlier modules were corrected by
                            accident when a later module re-globbed the tree.
                            verify runs after package, so the artifact is final.
                        -->
                        <phase>verify</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${maven.multiModuleProjectDirectory}/target/plugins" />
                                <copy todir="${maven.multiModuleProjectDirectory}/target/plugins" flatten="true">
                                    <fileset dir="${maven.multiModuleProjectDirectory}">
                                        <include name="**/target/*.jar" />
                                        <exclude name="target/**" />
                                        <exclude name="**/target/original-*.jar" />
                                        <exclude name="**/target/dependency-reduced-pom.xml" />
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    
</project>